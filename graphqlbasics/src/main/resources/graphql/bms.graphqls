# java.util.Date implementation

type Book {
        isbn:String!,
        title:String,
        price:Int
}

type Query {
    allBooks: [Book]
}

type Mutation {
    save(isbn:String!, title:String!,pubYear:String, price:Int): Boolean
}
